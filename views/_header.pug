header.header
  nav.nav.nav--tours
    a.nav__el(href="/") All tours
  .header__logo 
    img(src='/img/logo-white.png' alt='Natours logo')
  nav.nav.nav--user
    if user
      a(href="/" class="nav__el nav__el--logout" id="logBro") Log out
      a(href="/me" class="nav__el") 
          img(src=`/img/users/${user.photo}` alt=`photo of ${user.name}` class="nav__user-img")
          //- img(src=`/img/users/user-625150d5f9a1e2423098a152-1649954976069.jpeg` alt=`photo of ${user.name}` class="nav__user-img")
          span= user.name.split(' ')[0]
      //- button.nav__el 
    else
      a.nav__el(href='/login') Log in
      a.nav__el.nav__el--cta.signup(href='/signUp') Sign up

    //- form(class='nav__search') 
    //-     button(class='nav__search-btn')
    //-         svg:use(xlink:href='img/icons.svg#icon-search')
    //-     input(type='text' placeholder='Search tours' class='nav__search-input')